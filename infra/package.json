{
  "name": "pickle-play-dates-infra",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "bootstrap": "cdk bootstrap",
    "deploy:dev": "cdk deploy --all --context env=dev",
    "deploy:prod": "cdk deploy --all --context env=prod",
    "destroy:dev": "cdk destroy --all --context env=dev",
    "destroy:prod": "cdk destroy --all --context env=prod",
    "synth": "cdk synth",
    "diff": "cdk diff"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.19.9",
    "aws-cdk": "^2.110.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.110.0",
    "constructs": "^10.3.0",
    "dotenv": "^17.2.1",
    "esbuild": "^0.25.8"
  }
}
